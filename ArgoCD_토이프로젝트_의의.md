# ArgoCD 토이 프로젝트 — 기존 배포 방식 대비 유의미한 차이점

## 기존 배포 방식 (전통적 방식)

```
개발자가 코드 수정 → 서버에 SSH 접속 → git pull → 수동으로 재시작
```

- 사람이 직접 서버에 접속해서 배포
- 배포 과정이 문서화되지 않고, 담당자 머릿속에만 존재
- 롤백하려면 이전 버전을 기억하고 수동으로 되돌려야 함
- 서버가 여러 대면 하나하나 반복 작업

---

## 이 프로젝트에서 경험하는 방식 (GitOps + ArgoCD)

```
개발자가 코드 수정 → git push → Jenkins가 자동 빌드 → ArgoCD가 자동 배포
```

---

## 핵심적으로 달라지는 점

### 1. 배포가 자동화된다
| 기존 | ArgoCD |
|------|--------|
| SSH 접속 → git pull → 재시작 | git push만 하면 끝 |
| 배포할 때마다 사람이 개입 | Jenkins + ArgoCD가 전부 처리 |

**의미**: 배포에 드는 시간이 수십 분 → 수 분으로 줄어든다. 사람의 실수(잘못된 서버에 배포, 설정 누락 등)가 사라진다.

### 2. 인프라 상태가 Git에 기록된다 (GitOps)
| 기존 | ArgoCD |
|------|--------|
| 서버 상태를 알려면 직접 접속해서 확인 | `k8s/deployment.yaml`을 보면 현재 상태를 알 수 있음 |
| "지금 어떤 버전이 배포되어 있지?" → 서버 확인 | Git 히스토리 = 배포 히스토리 |

**의미**: 누가, 언제, 무엇을 배포했는지 Git 커밋으로 전부 추적 가능하다. 감사(Audit)와 협업이 쉬워진다.

### 3. 롤백이 간단해진다
| 기존 | ArgoCD |
|------|--------|
| 이전 버전 기억 → 수동 롤백 → 기도 | `git revert` → ArgoCD가 자동으로 이전 상태로 복구 |
| 롤백 자체가 리스크 | 롤백 = 그냥 Git 커밋 하나 되돌리기 |

**의미**: 장애 발생 시 복구 시간(MTTR)이 획기적으로 줄어든다.

### 4. 셀프 힐링 (Self-Heal)
| 기존 | ArgoCD |
|------|--------|
| 누군가 서버 설정을 임의로 바꾸면 그대로 유지 | ArgoCD가 Git과 다른 상태를 감지 → 자동으로 원래대로 복구 |

**의미**: "누가 서버 설정 건드렸어?"라는 상황이 사라진다. Git에 정의된 상태가 곧 진실(Single Source of Truth)이다.

### 5. 스케일링이 선언적이다
| 기존 | ArgoCD |
|------|--------|
| 트래픽 증가 → 서버 추가 → 수동 설정 | `replicas: 2` → `replicas: 5`로 변경 후 git push → 자동 반영 |

**의미**: 서버 증설/축소가 코드 한 줄 바꾸는 것만큼 쉬워진다.

---

## 이 토이 프로젝트에서 직접 체험하게 되는 것

| 단계 | 체험 내용 |
|------|-----------|
| Docker 이미지 빌드 | 애플리케이션을 컨테이너로 패키징하는 과정 |
| Jenkins CI 파이프라인 | 코드 푸시 → 자동 빌드 → 이미지 푸시 흐름 |
| Kubernetes 배포 | 컨테이너 오케스트레이션, 헬스체크, 레플리카 관리 |
| ArgoCD GitOps | Git 변경 감지 → 자동 동기화 → 셀프 힐링 |
| 전체 파이프라인 | CI/CD 전체 흐름을 end-to-end로 이해 |

---

## 한 줄 요약

> **"서버에 SSH 접속해서 배포하던 시대"에서 "git push만 하면 알아서 배포되는 시대"로의 전환을 직접 경험하는 프로젝트.**

---

## 실무에서의 가치

이 프로젝트의 기술 스택(Docker + K8s + Jenkins + ArgoCD)은 현재 대부분의 IT 기업에서 사용하는 표준 배포 파이프라인이다. 토이 프로젝트로 이 흐름을 한 번 경험해두면:

- DevOps/백엔드 면접에서 CI/CD 파이프라인을 구체적으로 설명할 수 있다
- 실무에서 기존 파이프라인을 이해하고 개선할 수 있다
- "배포는 다른 팀이 해주는 것"이 아니라 개발 프로세스의 일부로 인식하게 된다
